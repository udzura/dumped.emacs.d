;; -*- mode: emacs-lisp -*-
;; History entries used for anything adaptive display.
(setq anything-c-adaptive-history (quote ((command ("list-packages" ("list-pa" . 1) ("list-p" . 1)) ("list-buffers" ("list-bu" . 1))))))
